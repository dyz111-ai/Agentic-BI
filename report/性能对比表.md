# 预聚合视图性能对比

## 三组对比汇总

| 场景 | 预聚合表 | Raw JOIN 平均 (ms) | Pre-agg 平均 (ms) | 加速倍数 |
|------|----------|-------------------:|------------------:|---------:|
| 各州 GMV 排名 | `mv_state_sales` | 1349.59 | 1.15 | **1171.62×** |
| 月度 GMV 趋势 | `mv_monthly_sales` | 758.25 | 1.17 | **646.16×** |
| 支付方式分布 | `mv_payment_dist` | 858.34 | 1.05 | **814.29×** |

> 结论：相同业务语义下，直接查询预聚合表相比实时多表 JOIN 聚合，响应时间从 **数百～1300 ms** 降至 **约 1 ms**，加速 **600～1100 倍**。

---

## 分组明细

### 1. 各州 GMV 排名（Raw JOIN vs `mv_state_sales`）

| 轮次 | Raw JOIN (ms) | Pre-agg (ms) |
|-----:|--------------:|-------------:|
| 1 | 1257.70 | 1.24 |
| 2 | 1333.10 | 1.25 |
| 3 | 1457.96 | 0.97 |
| **平均** | **1349.59** | **1.15** |

### 2. 月度 GMV 趋势（Raw JOIN vs `mv_monthly_sales`）

| 轮次 | Raw JOIN (ms) | Pre-agg (ms) |
|-----:|--------------:|-------------:|
| 1 | 751.11 | 1.27 |
| 2 | 748.43 | 1.22 |
| 3 | 775.22 | 1.03 |
| **平均** | **758.25** | **1.17** |

### 3. 支付方式分布（Raw JOIN vs `mv_payment_dist`）

| 轮次 | Raw JOIN (ms) | Pre-agg (ms) |
|-----:|--------------:|-------------:|
| 1 | 848.95 | 1.02 |
| 2 | 863.63 | 1.05 |
| 3 | 862.45 | 1.09 |
| **平均** | **858.34** | **1.05** |

---

## 预聚合表单独测速（仅 SELECT）

| 预聚合表 | 平均 (ms) | 最小 (ms) | 最大 (ms) |
|----------|----------:|----------:|----------:|
| `mv_monthly_sales` | 0.75 | 0.70 | 0.85 |
| `mv_state_sales` | 0.75 | 0.72 | 0.78 |
| `mv_category_sales` | 0.98 | 0.88 | 1.04 |
| `mv_delivery_perf` | 0.95 | 0.80 | 1.13 |
| `mv_payment_dist` | 0.91 | 0.74 | 1.19 |

---

## 数据正确性校验

| 项目 | 结果 |
|------|------|
| 校验方式 | 月度 GMV：Raw `orders + payments` 聚合 vs `mv_monthly_sales` |
| 对比行数 | 25 |
| 最大偏差 | 0.0000 |
| 结论 | ✓ 预聚合结果与原始表聚合一致 |

---

## 对比 SQL 示例

### 各州 GMV

**Raw JOIN（实时多表聚合）：**

```sql
SELECT
    strftime('%Y-%m', o.order_purchase_timestamp) AS year_month,
    c.customer_state,
    ROUND(SUM(p.payment_value), 2) AS total_gmv,
    COUNT(DISTINCT o.order_id) AS total_orders
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
JOIN payments p ON o.order_id = p.order_id
WHERE o.order_purchase_timestamp IS NOT NULL
GROUP BY strftime('%Y-%m', o.order_purchase_timestamp), c.customer_state
ORDER BY total_gmv DESC
LIMIT 20;
```

**Pre-aggregation：**

```sql
SELECT year_month, customer_state, total_gmv, total_orders, unique_customers
FROM mv_state_sales
ORDER BY total_gmv DESC
LIMIT 20;
```

### 月度 GMV

**Raw JOIN：**

```sql
SELECT
    strftime('%Y-%m', o.order_purchase_timestamp) AS year_month,
    ROUND(SUM(p.payment_value), 2) AS total_gmv,
    COUNT(DISTINCT o.order_id) AS total_orders
FROM orders o
JOIN payments p ON o.order_id = p.order_id
WHERE o.order_purchase_timestamp IS NOT NULL
GROUP BY strftime('%Y-%m', o.order_purchase_timestamp)
ORDER BY year_month;
```

**Pre-aggregation：**

```sql
SELECT year_month, total_gmv, total_orders, avg_basket, total_freight
FROM mv_monthly_sales
ORDER BY year_month;
```

### 支付方式分布

**Raw JOIN：**

```sql
SELECT
    strftime('%Y-%m', o.order_purchase_timestamp) AS year_month,
    p.payment_type,
    COUNT(*) AS total_transactions,
    ROUND(AVG(p.payment_installments), 2) AS avg_installments,
    ROUND(SUM(p.payment_value), 2) AS total_value
FROM payments p
JOIN orders o ON p.order_id = o.order_id
WHERE o.order_purchase_timestamp IS NOT NULL
GROUP BY strftime('%Y-%m', o.order_purchase_timestamp), p.payment_type
ORDER BY total_value DESC
LIMIT 20;
```

**Pre-aggregation：**

```sql
SELECT year_month, payment_type, total_transactions, avg_installments, total_value
FROM mv_payment_dist
ORDER BY total_value DESC
LIMIT 20;
```
